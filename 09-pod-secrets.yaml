apiVersion: v1
kind: Pod
metadata:
  name: k8-secrets
  labels:
    topic: secret
    tool: k8
spec:
  containers:
  - name: httpd
    image: httpd:latest
    command: ["httpd", "-DFOREGROUND"]
    envFrom:
    - secretRef:
        name: pod-secrets
